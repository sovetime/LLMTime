server:
  port: 8888
  servlet:
    encoding:
      charset: UTF-8
      force: true
      enabled: true
spring:
  ai:
    openai:
      api-key: @dashscope.api.key@
      base-url: https://dashscope.aliyuncs.com/compatible-mode/
      chat:
        options:
          model: qwen-plus
          temperature: 0.7
          maxTokens: 5000

      embedding:
        base-url: https://dashscope.aliyuncs.com/compatible-mode/
        api-key: @dashscope.api.key@
        options:
          model: text-embedding-v4
          dimmension: 1024

  datasource:
    url: jdbc:mysql://127.0.0.1:3307/spring_ai?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=GMT%2B8 # 记得改成你自己的
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
    # 连接池配置
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      idle-timeout: 60000
      connection-timeout: 30000
      max-lifetime: 1800000

  # Redis 配置
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      timeout: 5s
      password: 123456

  # MyBatis-Plus 配置
  mybatis-plus:
    mapper-locations: classpath:mapper/*.xml
  servlet:
    multipart:
      enabled: true
      max-file-size: 50MB
      max-request-size: 50MB

minio:
  url: http://localhost:9000/
  accessKey: minioadmin
  secretKey: minioadmin
  bucketName: dodo-agent
  endpoint: http://localhost:9000/

embeddings:
  store:
    host: localhost
    port: 5432
    database: rag_test
    user: pgvector
    password: pgvector

# 秘塔搜索配置
metaso:
  api-key: mk-84529ADC83175BB469C4461BF2DDA39A
  search-url: https://metaso.cn/api/v1/search
  scope: webpage
  size: 10
  include-summary: false
  include-raw-content: false
  concise-snippet: false

grsai:
  nanobanana:
    api-key: sk-ec99XXXXXXXXXXXXXX # 记得改成你自己的

logging:
  level:
    org:
      springframework:
        ai: INFO
    io:
      modelcontextprotocol:
        client: ERROR
