server:
  port: 8013
spring:
  application:
    name: function-call
  ai:
    openai:
      api-key: @dashscope.api.key@
      base-url: https://dashscope.aliyuncs.com/compatible-mode/
      chat:
        options:
          model: glm-4.7
          temperature: 0.7
          maxTokens: 5000
